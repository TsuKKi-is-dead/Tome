{
  "name": "tome",
  "version": "1.0.0",
  "description": "Tome - book search, covers, ratings & tracking for Obsidian",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "builtin-modules": "^5.3.0",
    "esbuild": "0.19.11",
    "obsidian": "latest",
    "tslib": "^2.8.1",
    "typescript": "5.3.3"
  }
}
